---
---

<section class="hero">
  <div class="container">
    <div class="hero-content">
      <h1>Davi Rezende transformando<span> o  descartado</span>
        <span> em Arte</span></h1>
      <p>Soluções criativas de upcycling que combinam sustentabilidade e design exclusivo</p>
      <div class="hero-buttons">
        <a href="#trabalhos" class="btn">Ver Trabalhos</a>
        <a href="https://api.whatsapp.com/send?phone=5521982221872&text=oi%2C%20vim%20do%20seu%20site" class="btn btn-outline" target="_blank" rel="noopener noreferrer">Contato</a>
      </div>
    </div>
    <div class="hero-image">
      <div class="image-container">
        <img src="/images/arts/IMG_2686.JPG" alt="Upcycle Design" />
        <div class="image-overlay"></div>
        <div class="image-accent"></div>
      </div>
    </div>
  </div>
  <div class="scroll-indicator">
    <div class="mouse">
      <div class="wheel"></div>
    </div>
    <div>
      <span>Role para baixo</span>
    </div>
  </div>
</section>

<style>
  .hero {
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    position: relative;
    overflow: hidden;
  }
  
  .hero .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    align-items: center;
  }
  
  .hero-content {
    z-index: 1;
  }
  
  .hero-content h1 {
    font-size: 3.5rem;
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 1.5rem;
  }
  
  .hero-content h1 span {
    position: relative;
    display: inline-block;
  }
  
  .hero-content h1 span::after {
    content: '';
    position: absolute;
    bottom: 5px;
    left: 0;
    width: 100%;
    height: 8px;
    background-color: rgba(0, 0, 0, 0.1);
    z-index: -1;
  }
  
  .hero-content p {
    font-size: 1.2rem;
    margin-bottom: 2rem;
    max-width: 500px;
  }
  
  .hero-buttons {
    display: flex;
    gap: 1rem;
  }
  
  .hero-image {
    position: relative;
    height: 100%;
    width: 100%;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 20px;
  }
  
  .image-container {
    position: relative;
    overflow: hidden;
    /* border-radius: 20px 100px 20px 100px; */
    height: 90vh;  /* Ajustado para melhor proporção */
    max-height: 700px;  /* Aumentado para mostrar mais conteúdo */
    width: 120%;  /* Aumentado para melhor proporção */
    transform: translateY(-10px);
    background-color: rgba(255, 255, 255, 0.05);
    
  }
  
  .image-container::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    /* background: linear-gradient(to bottom, rgba(0, 0, 0, 0), rgba(0, 0, 0, 0.05)); */
    z-index: 1;
  }
  
  /* .image-container::after {
    content: '';
    position: absolute;
    top: -5px;
    left: -5px;
    right: -5px;
    bottom: -5px;
    border: 2px solid rgba(255, 255, 255, 0.2);
    border-radius: 25px 105px 25px 105px;
    z-index: 0;
    pointer-events: none;
  } */
  
  .image-container img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain; /* Alterado de 'cover' para 'contain' */
    object-position: center;
    transition: transform 0.7s ease;
  }
  
  .image-container:hover img {
    transform: scale(1.05);
  }
  
  .scroll-indicator {
    position: absolute;
    bottom: 3rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    opacity: 0.7;
  }
  
  .mouse {
    width: 26px;
    height: 40px;
    border: 2px solid var(--color-primary);
    border-radius: 20px;
    position: relative;
  }
  
  .wheel {
    width: 4px;
    height: 8px;
    background-color: var(--color-primary);
    position: absolute;
    top: 7px;
    left: 50%;
    transform: translateX(-50%);
    border-radius: 2px;
    animation: scroll 1.5s infinite;
  }
  
  @keyframes scroll {
    0% {
      opacity: 1;
      transform: translateX(-50%) translateY(0);
    }
    100% {
      opacity: 0;
      transform: translateX(-50%) translateY(15px);
    }
  }
  
  @media (max-width: 992px) {
    .hero .container {
      grid-template-columns: 1fr;
      text-align: center;
    }
    
    .hero-content h1 {
      font-size: 2.5rem;
    }
    
    .hero-content p {
      margin: 0 auto 2rem;
    }
    
    .hero-buttons {
      justify-content: center;
    }
    
    .hero-image {
      display: none;
    }
  }
</style>